- name: conditions
  hosts: localhost
  become: yes
  tasks:
  - name: create user
    ansible.builtin.command: id expense
    ignore_errors: true
  - name:
    ansible.builtin.debug:
     msg: "hello world"  