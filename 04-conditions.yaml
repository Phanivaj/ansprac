- name: conditions
  hosts: localhost
  become: yes
  tasks:
  - name: create user
    ansible.builtin.command: id expense
    register: user
    ignore_errors: true
  - name: print user info
    ansible.builtin.debug:
     msg: user info is {{user}} 
  - name:
    ansible.builtin.debug:
     msg: "hello world"  